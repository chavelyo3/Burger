  <li>
	{{burger_name}}
{{#unless devoured}}
			<button class="burger-eaten" data-id="{{id}}" data-newOrder="{{devoured}}">
		{{#if devoured}}lets eat!{{else}}lets eat!{{/if}}
	</button>
			
			{{/unless}}
	

	<button class="delete-burger" data-id="{{this.id}}">Delete!</button>
</li>
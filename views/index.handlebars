<h1>Burgers!</h1>

<h2>yo quiero hamburguesas</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burgersacomer}}
    {{/unless}}
  {{/each}}
</ul>

<h2>hamburguesas eaten</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burgerseaten}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger type:</label>
    <input type="text" id="ca" name="name">
  </div>


  <button type="submit">Add a burger</button>
</form>
